# Copy this file to .env.local and adjust as needed.
# .env.local is gitignored.
#
# Base URL of the QuickBite API Gateway (Alfa-Kilo). Phase 12 routes all
# requests through this. The frontend itself runs on :8090.
VUE_APP_API_BASE_URL=http://localhost:8080
