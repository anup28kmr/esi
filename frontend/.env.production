# Production build.
#
# The browser calls the Spring Cloud Gateway (api-gateway) directly on
# its host-published port. There is no nginx reverse proxy in between;
# the frontend container only serves the SPA's static assets. The
# gateway is responsible for CORS.
VUE_APP_API_BASE_URL=http://localhost:9090
