# Production build (Docker / nginx).
#
# Empty value = same-origin. The frontend makes requests to `/api/**`
# on its own nginx origin; the nginx config in the Docker image
# proxies those to the API Gateway service on the compose network.
# Keeps the browser out of CORS territory for the packaged demo.
VUE_APP_API_BASE_URL=
